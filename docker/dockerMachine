# docker machine install URL
https://github.com/docker/machine/releases/download/v0.13.0/docker-machine-Linux-x86_64

# to list available docker machines
docker-machine ls

# This command downloads a lightweight Linux distribution (boot2docker) with the Docker daemon installed,and creates and starts a VirtualBox VM with Docker running
# --driver : flag to indicate which provider
docker-machine create --driver virtualbox default

# As noted in the output of the docker-machine create command
docker-machine env default

# This sets environment variables for the current shell that the Docker client will read which specify the TLS settings. You need to do this each time you open a new shell or restart your machine.
eval "$(docker-machine env default)"

# Get the host IP address
docker-machine ip default

# Start stop and restart machines
docker-machine stop default
docker-machine start default
docker-machine restart default

# List all the details of a machine
docker-machine inspect

# Re-run provisioning on a created machine
docker-machine provision

# Get the URL of a host
docker-machine url default

docker-machine config
docker-machine status
docker-machine kill
docker-machine rm
